---
title: "endstone.scoreboard"
---

## <code className="doc-symbol doc-symbol-module"/> endstone.scoreboard [#endstone.scoreboard]

**Classes:**

Name | Description
---- | -----------
[`Criteria`](#endstone.scoreboard.Criteria) | Represents a scoreboard criteria.
[`DisplaySlot`](#endstone.scoreboard.DisplaySlot) | Locations for displaying objectives to the player
[`Objective`](#endstone.scoreboard.Objective) | Represents an objective on a scoreboard that can show scores specific to entries.
[`ObjectiveSortOrder`](#endstone.scoreboard.ObjectiveSortOrder) | Represents the sort order of objectives on a DisplaySlot.
[`Score`](#endstone.scoreboard.Score) | Represents a score for an objective on a scoreboard.
[`Scoreboard`](#endstone.scoreboard.Scoreboard) | Represents a scoreboard

### <code className="doc-symbol doc-symbol-class"/> Criteria [#endstone.scoreboard.Criteria]

<div className="pl-[2em] mt-[1.25em]">

Represents a scoreboard criteria.

**Classes:**

Name | Description
---- | -----------
[`Type`](#endstone.scoreboard.Criteria.Type) | Represents a scoreboard criteria.

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="DUMMY">
        <td>[`DUMMY`](#endstone.scoreboard.Criteria.DUMMY)</td>
        <td><code className="nextra-code">[Type](#endstone._internal.endstone_python.Criteria.Type)</code></td>
        <td></td>
    </tr>
    <tr key="default_render_type">
        <td>[`default_render_type`](#endstone.scoreboard.Criteria.default_render_type)</td>
        <td><code className="nextra-code">[RenderType](#endstone._internal.endstone_python.RenderType)</code></td>
        <td></td>
    </tr>
    <tr key="is_read_only">
        <td>[`is_read_only`](#endstone.scoreboard.Criteria.is_read_only)</td>
        <td><code className="nextra-code">[bool](#bool)</code></td>
        <td></td>
    </tr>
    <tr key="name">
        <td>[`name`](#endstone.scoreboard.Criteria.name)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td></td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> DUMMY [#endstone.scoreboard.Criteria.DUMMY]

```python
DUMMY: Criteria.Type
```

#### <code className="doc-symbol doc-symbol-class"/> Type [#endstone.scoreboard.Criteria.Type]

```python
Type(value:int)
```

<div className="pl-[2em] mt-[1.25em]">

Represents a scoreboard criteria.

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="DUMMY">
        <td>[`DUMMY`](#endstone.scoreboard.Criteria.Type.DUMMY)</td>
        <td><code className="nextra-code">[Type](#endstone._internal.endstone_python.Criteria.Type)</code></td>
        <td></td>
    </tr>
    <tr key="name">
        <td>[`name`](#endstone.scoreboard.Criteria.Type.name)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td></td>
    </tr>
    <tr key="value">
        <td>[`value`](#endstone.scoreboard.Criteria.Type.value)</td>
        <td><code className="nextra-code">[int](#int)</code></td>
        <td></td>
    </tr></tbody>
</table>

##### <code className="doc-symbol doc-symbol-attribute"/> DUMMY [#endstone.scoreboard.Criteria.Type.DUMMY]

```python
DUMMY: Criteria.Type
```

##### <code className="doc-symbol doc-symbol-attribute"/> name [#endstone.scoreboard.Criteria.Type.name]

```python
name: str
```

##### <code className="doc-symbol doc-symbol-attribute"/> value [#endstone.scoreboard.Criteria.Type.value]

```python
value: int
```

</div>

#### <code className="doc-symbol doc-symbol-attribute"/> default_render_type [#endstone.scoreboard.Criteria.default_render_type]

```python
default_render_type: RenderType
```

#### <code className="doc-symbol doc-symbol-attribute"/> is_read_only [#endstone.scoreboard.Criteria.is_read_only]

```python
is_read_only: bool
```

#### <code className="doc-symbol doc-symbol-attribute"/> name [#endstone.scoreboard.Criteria.name]

```python
name: str
```

</div>

### <code className="doc-symbol doc-symbol-class"/> DisplaySlot [#endstone.scoreboard.DisplaySlot]

```python
DisplaySlot(value:int)
```

<div className="pl-[2em] mt-[1.25em]">

Locations for displaying objectives to the player

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="BELOW_NAME">
        <td>[`BELOW_NAME`](#endstone.scoreboard.DisplaySlot.BELOW_NAME)</td>
        <td><code className="nextra-code">[DisplaySlot](#endstone._internal.endstone_python.DisplaySlot)</code></td>
        <td></td>
    </tr>
    <tr key="PLAYER_LIST">
        <td>[`PLAYER_LIST`](#endstone.scoreboard.DisplaySlot.PLAYER_LIST)</td>
        <td><code className="nextra-code">[DisplaySlot](#endstone._internal.endstone_python.DisplaySlot)</code></td>
        <td></td>
    </tr>
    <tr key="SIDE_BAR">
        <td>[`SIDE_BAR`](#endstone.scoreboard.DisplaySlot.SIDE_BAR)</td>
        <td><code className="nextra-code">[DisplaySlot](#endstone._internal.endstone_python.DisplaySlot)</code></td>
        <td></td>
    </tr>
    <tr key="name">
        <td>[`name`](#endstone.scoreboard.DisplaySlot.name)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td></td>
    </tr>
    <tr key="value">
        <td>[`value`](#endstone.scoreboard.DisplaySlot.value)</td>
        <td><code className="nextra-code">[int](#int)</code></td>
        <td></td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> BELOW_NAME [#endstone.scoreboard.DisplaySlot.BELOW_NAME]

```python
BELOW_NAME: DisplaySlot
```

#### <code className="doc-symbol doc-symbol-attribute"/> PLAYER_LIST [#endstone.scoreboard.DisplaySlot.PLAYER_LIST]

```python
PLAYER_LIST: DisplaySlot
```

#### <code className="doc-symbol doc-symbol-attribute"/> SIDE_BAR [#endstone.scoreboard.DisplaySlot.SIDE_BAR]

```python
SIDE_BAR: DisplaySlot
```

#### <code className="doc-symbol doc-symbol-attribute"/> name [#endstone.scoreboard.DisplaySlot.name]

```python
name: str
```

#### <code className="doc-symbol doc-symbol-attribute"/> value [#endstone.scoreboard.DisplaySlot.value]

```python
value: int
```

</div>

### <code className="doc-symbol doc-symbol-class"/> Objective [#endstone.scoreboard.Objective]

<div className="pl-[2em] mt-[1.25em]">

Represents an objective on a scoreboard that can show scores specific to entries.

**Methods:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="get_score">
        <td>[`get_score`](#endstone.scoreboard.Objective.get_score)</td>
        <td>Gets an entry's Score for this objective</td>
    </tr>
    <tr key="set_display">
        <td>[`set_display`](#endstone.scoreboard.Objective.set_display)</td>
        <td>Sets the display slot and sort order for this objective. This will remove it from any other display slot.</td>
    </tr>
    <tr key="unregister">
        <td>[`unregister`](#endstone.scoreboard.Objective.unregister)</td>
        <td>Unregisters this objective from the associated Scoreboard.</td>
    </tr></tbody>
</table>

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="criteria">
        <td>[`criteria`](#endstone.scoreboard.Objective.criteria)</td>
        <td><code className="nextra-code">[Criteria](#endstone._internal.endstone_python.Criteria)</code></td>
        <td>Gets the criteria this objective tracks</td>
    </tr>
    <tr key="display_name">
        <td>[`display_name`](#endstone.scoreboard.Objective.display_name)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td>Gets or sets the name displayed to players for this objective</td>
    </tr>
    <tr key="display_slot">
        <td>[`display_slot`](#endstone.scoreboard.Objective.display_slot)</td>
        <td><code className="nextra-code">[DisplaySlot](#endstone._internal.endstone_python.DisplaySlot) | None</code></td>
        <td>Gets or sets the display slot this objective is displayed at</td>
    </tr>
    <tr key="is_displayed">
        <td>[`is_displayed`](#endstone.scoreboard.Objective.is_displayed)</td>
        <td><code className="nextra-code">[bool](#bool)</code></td>
        <td>Gets if the objective is currently displayed in a slot.</td>
    </tr>
    <tr key="is_modifiable">
        <td>[`is_modifiable`](#endstone.scoreboard.Objective.is_modifiable)</td>
        <td><code className="nextra-code">[bool](#bool)</code></td>
        <td>Gets if the objective's scores can be modified directly by a plugin</td>
    </tr>
    <tr key="name">
        <td>[`name`](#endstone.scoreboard.Objective.name)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td>Gets the name of this Objective</td>
    </tr>
    <tr key="render_type">
        <td>[`render_type`](#endstone.scoreboard.Objective.render_type)</td>
        <td><code className="nextra-code">[RenderType](#endstone._internal.endstone_python.RenderType)</code></td>
        <td>Gets the manner in which this objective will be rendered.</td>
    </tr>
    <tr key="scoreboard">
        <td>[`scoreboard`](#endstone.scoreboard.Objective.scoreboard)</td>
        <td><code className="nextra-code">[Scoreboard](#endstone._internal.endstone_python.Scoreboard)</code></td>
        <td>Gets the scoreboard to which this objective is attached</td>
    </tr>
    <tr key="sort_order">
        <td>[`sort_order`](#endstone.scoreboard.Objective.sort_order)</td>
        <td><code className="nextra-code">[ObjectiveSortOrder](#endstone._internal.endstone_python.ObjectiveSortOrder) | None</code></td>
        <td>Gets or sets the sort order for this objective</td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> criteria [#endstone.scoreboard.Objective.criteria]

```python
criteria: Criteria
```

Gets the criteria this objective tracks

#### <code className="doc-symbol doc-symbol-attribute"/> display_name [#endstone.scoreboard.Objective.display_name]

```python
display_name: str
```

Gets or sets the name displayed to players for this objective

#### <code className="doc-symbol doc-symbol-attribute"/> display_slot [#endstone.scoreboard.Objective.display_slot]

```python
display_slot: DisplaySlot | None
```

Gets or sets the display slot this objective is displayed at

#### <code className="doc-symbol doc-symbol-method"/> get_score [#endstone.scoreboard.Objective.get_score]

```python
get_score(entry:Player | Actor | str) -> Score
```

Gets an entry's Score for this objective

#### <code className="doc-symbol doc-symbol-attribute"/> is_displayed [#endstone.scoreboard.Objective.is_displayed]

```python
is_displayed: bool
```

Gets if the objective is currently displayed in a slot.

#### <code className="doc-symbol doc-symbol-attribute"/> is_modifiable [#endstone.scoreboard.Objective.is_modifiable]

```python
is_modifiable: bool
```

Gets if the objective's scores can be modified directly by a plugin

#### <code className="doc-symbol doc-symbol-attribute"/> name [#endstone.scoreboard.Objective.name]

```python
name: str
```

Gets the name of this Objective

#### <code className="doc-symbol doc-symbol-attribute"/> render_type [#endstone.scoreboard.Objective.render_type]

```python
render_type: RenderType
```

Gets the manner in which this objective will be rendered.

#### <code className="doc-symbol doc-symbol-attribute"/> scoreboard [#endstone.scoreboard.Objective.scoreboard]

```python
scoreboard: Scoreboard
```

Gets the scoreboard to which this objective is attached

#### <code className="doc-symbol doc-symbol-method"/> set_display [#endstone.scoreboard.Objective.set_display]

```python
set_display(
    slot: DisplaySlot | None, order: ObjectiveSortOrder | None = None
) -> None
```

Sets the display slot and sort order for this objective. This will remove it from any other display slot.

#### <code className="doc-symbol doc-symbol-attribute"/> sort_order [#endstone.scoreboard.Objective.sort_order]

```python
sort_order: ObjectiveSortOrder | None
```

Gets or sets the sort order for this objective

#### <code className="doc-symbol doc-symbol-method"/> unregister [#endstone.scoreboard.Objective.unregister]

```python
unregister() -> None
```

Unregisters this objective from the associated Scoreboard.

</div>

### <code className="doc-symbol doc-symbol-class"/> ObjectiveSortOrder [#endstone.scoreboard.ObjectiveSortOrder]

```python
ObjectiveSortOrder(value:int)
```

<div className="pl-[2em] mt-[1.25em]">

Represents the sort order of objectives on a DisplaySlot.

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="ASCENDING">
        <td>[`ASCENDING`](#endstone.scoreboard.ObjectiveSortOrder.ASCENDING)</td>
        <td><code className="nextra-code">[ObjectiveSortOrder](#endstone._internal.endstone_python.ObjectiveSortOrder)</code></td>
        <td></td>
    </tr>
    <tr key="DESCENDING">
        <td>[`DESCENDING`](#endstone.scoreboard.ObjectiveSortOrder.DESCENDING)</td>
        <td><code className="nextra-code">[ObjectiveSortOrder](#endstone._internal.endstone_python.ObjectiveSortOrder)</code></td>
        <td></td>
    </tr>
    <tr key="name">
        <td>[`name`](#endstone.scoreboard.ObjectiveSortOrder.name)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td></td>
    </tr>
    <tr key="value">
        <td>[`value`](#endstone.scoreboard.ObjectiveSortOrder.value)</td>
        <td><code className="nextra-code">[int](#int)</code></td>
        <td></td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> ASCENDING [#endstone.scoreboard.ObjectiveSortOrder.ASCENDING]

```python
ASCENDING: ObjectiveSortOrder
```

#### <code className="doc-symbol doc-symbol-attribute"/> DESCENDING [#endstone.scoreboard.ObjectiveSortOrder.DESCENDING]

```python
DESCENDING: ObjectiveSortOrder
```

#### <code className="doc-symbol doc-symbol-attribute"/> name [#endstone.scoreboard.ObjectiveSortOrder.name]

```python
name: str
```

#### <code className="doc-symbol doc-symbol-attribute"/> value [#endstone.scoreboard.ObjectiveSortOrder.value]

```python
value: int
```

</div>

### <code className="doc-symbol doc-symbol-class"/> Score [#endstone.scoreboard.Score]

<div className="pl-[2em] mt-[1.25em]">

Represents a score for an objective on a scoreboard.

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="entry">
        <td>[`entry`](#endstone.scoreboard.Score.entry)</td>
        <td><code className="nextra-code">[Player](#endstone._internal.endstone_python.Player) | [Actor](#endstone._internal.endstone_python.Actor) | [str](#str)</code></td>
        <td>Gets the entry being tracked by this Score</td>
    </tr>
    <tr key="is_score_set">
        <td>[`is_score_set`](#endstone.scoreboard.Score.is_score_set)</td>
        <td><code className="nextra-code">[bool](#bool)</code></td>
        <td>Shows if this score has been set at any point in time.</td>
    </tr>
    <tr key="objective">
        <td>[`objective`](#endstone.scoreboard.Score.objective)</td>
        <td><code className="nextra-code">[Objective](#endstone._internal.endstone_python.Objective)</code></td>
        <td>Gets the Objective being tracked by this Score.</td>
    </tr>
    <tr key="scoreboard">
        <td>[`scoreboard`](#endstone.scoreboard.Score.scoreboard)</td>
        <td><code className="nextra-code">[Scoreboard](#endstone._internal.endstone_python.Scoreboard)</code></td>
        <td>Gets the scoreboard for the associated objective.</td>
    </tr>
    <tr key="value">
        <td>[`value`](#endstone.scoreboard.Score.value)</td>
        <td><code className="nextra-code">[int](#int)</code></td>
        <td>Gets or sets the current score.</td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> entry [#endstone.scoreboard.Score.entry]

```python
entry: Player | Actor | str
```

Gets the entry being tracked by this Score

#### <code className="doc-symbol doc-symbol-attribute"/> is_score_set [#endstone.scoreboard.Score.is_score_set]

```python
is_score_set: bool
```

Shows if this score has been set at any point in time.

#### <code className="doc-symbol doc-symbol-attribute"/> objective [#endstone.scoreboard.Score.objective]

```python
objective: Objective
```

Gets the Objective being tracked by this Score.

#### <code className="doc-symbol doc-symbol-attribute"/> scoreboard [#endstone.scoreboard.Score.scoreboard]

```python
scoreboard: Scoreboard
```

Gets the scoreboard for the associated objective.

#### <code className="doc-symbol doc-symbol-attribute"/> value [#endstone.scoreboard.Score.value]

```python
value: int
```

Gets or sets the current score.

</div>

### <code className="doc-symbol doc-symbol-class"/> Scoreboard [#endstone.scoreboard.Scoreboard]

<div className="pl-[2em] mt-[1.25em]">

Represents a scoreboard

**Methods:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="add_objective">
        <td>[`add_objective`](#endstone.scoreboard.Scoreboard.add_objective)</td>
        <td>Registers an Objective on this Scoreboard with a name displayed to players</td>
    </tr>
    <tr key="clear_slot">
        <td>[`clear_slot`](#endstone.scoreboard.Scoreboard.clear_slot)</td>
        <td>Clears any objective in the specified slot</td>
    </tr>
    <tr key="get_objective">
        <td>[`get_objective`](#endstone.scoreboard.Scoreboard.get_objective)</td>
        <td></td>
    </tr>
    <tr key="get_objectives_by_criteria">
        <td>[`get_objectives_by_criteria`](#endstone.scoreboard.Scoreboard.get_objectives_by_criteria)</td>
        <td>Gets all Objectives of a Criteria on the Scoreboard</td>
    </tr>
    <tr key="get_scores">
        <td>[`get_scores`](#endstone.scoreboard.Scoreboard.get_scores)</td>
        <td>Gets all scores for an entry on this Scoreboard</td>
    </tr>
    <tr key="reset_scores">
        <td>[`reset_scores`](#endstone.scoreboard.Scoreboard.reset_scores)</td>
        <td>Removes all scores for an entry on this Scoreboard</td>
    </tr></tbody>
</table>

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="entries">
        <td>[`entries`](#endstone.scoreboard.Scoreboard.entries)</td>
        <td><code className="nextra-code">[list](#list)[[Player](#endstone._internal.endstone_python.Player) | [Actor](#endstone._internal.endstone_python.Actor) | [str](#str)]</code></td>
        <td>Gets all entries tracked by this Scoreboard</td>
    </tr>
    <tr key="objectives">
        <td>[`objectives`](#endstone.scoreboard.Scoreboard.objectives)</td>
        <td><code className="nextra-code">[list](#list)[[Objective](#endstone._internal.endstone_python.Objective)]</code></td>
        <td>Gets all Objectives on this Scoreboard</td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-method"/> add_objective [#endstone.scoreboard.Scoreboard.add_objective]

```python
add_objective(
    name: str,
    criteria: Type,
    display_name: str | None = None,
    render_type: RenderType = RenderType.INTEGER,
) -> Objective
```

Registers an Objective on this Scoreboard with a name displayed to players

#### <code className="doc-symbol doc-symbol-method"/> clear_slot [#endstone.scoreboard.Scoreboard.clear_slot]

```python
clear_slot(slot:DisplaySlot) -> None
```

Clears any objective in the specified slot

#### <code className="doc-symbol doc-symbol-attribute"/> entries [#endstone.scoreboard.Scoreboard.entries]

```python
entries: list[Player | Actor | str]
```

Gets all entries tracked by this Scoreboard

#### <code className="doc-symbol doc-symbol-method"/> get_objective [#endstone.scoreboard.Scoreboard.get_objective]

```python
get_objective()
```

#### <code className="doc-symbol doc-symbol-method"/> get_objectives_by_criteria [#endstone.scoreboard.Scoreboard.get_objectives_by_criteria]

```python
get_objectives_by_criteria(criteria:Type) -> list[Objective]
```

Gets all Objectives of a Criteria on the Scoreboard

#### <code className="doc-symbol doc-symbol-method"/> get_scores [#endstone.scoreboard.Scoreboard.get_scores]

```python
get_scores(entry:Player | Actor | str) -> list[Score]
```

Gets all scores for an entry on this Scoreboard

#### <code className="doc-symbol doc-symbol-attribute"/> objectives [#endstone.scoreboard.Scoreboard.objectives]

```python
objectives: list[Objective]
```

Gets all Objectives on this Scoreboard

#### <code className="doc-symbol doc-symbol-method"/> reset_scores [#endstone.scoreboard.Scoreboard.reset_scores]

```python
reset_scores(entry:Player | Actor | str) -> None
```

Removes all scores for an entry on this Scoreboard

</div>
