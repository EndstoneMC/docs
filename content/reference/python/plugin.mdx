---
title: "endstone.plugin"
---

## <code className="doc-symbol doc-symbol-module"/> endstone.plugin [#endstone.plugin]

**Classes:**

Name | Description
---- | -----------
[`Plugin`](#endstone.plugin.Plugin) |
[`PluginCommand`](#endstone.plugin.PluginCommand) | Represents a Command belonging to a Plugin
[`PluginDescription`](#endstone.plugin.PluginDescription) | Represents the basic information about a plugin that the plugin loader needs to know.
[`PluginLoadOrder`](#endstone.plugin.PluginLoadOrder) | Represents the order in which a plugin should be initialized and enabled.
[`PluginLoader`](#endstone.plugin.PluginLoader) | Represents a plugin loader, which handles direct access to specific types of plugins
[`PluginManager`](#endstone.plugin.PluginManager) | Represents a plugin manager that handles all plugins from the Server
[`Service`](#endstone.plugin.Service) | Represents a list of methods.
[`ServiceManager`](#endstone.plugin.ServiceManager) | Represent a service manager that manages services and service providers.
[`ServicePriority`](#endstone.plugin.ServicePriority) | Represents various priorities of a provider.

### <code className="doc-symbol doc-symbol-class"/> Plugin [#endstone.plugin.Plugin]

```python
Plugin()
```

<div className="pl-[2em] mt-[1.25em]">

Bases: <code className="nextra-code">[Plugin](#endstone._internal.endstone_python.Plugin)</code>

**Methods:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="register_events">
        <td>[`register_events`](#endstone.plugin.Plugin.register_events)</td>
        <td></td>
    </tr>
    <tr key="reload_config">
        <td>[`reload_config`](#endstone.plugin.Plugin.reload_config)</td>
        <td></td>
    </tr>
    <tr key="save_config">
        <td>[`save_config`](#endstone.plugin.Plugin.save_config)</td>
        <td></td>
    </tr>
    <tr key="save_default_config">
        <td>[`save_default_config`](#endstone.plugin.Plugin.save_default_config)</td>
        <td></td>
    </tr>
    <tr key="save_resources">
        <td>[`save_resources`](#endstone.plugin.Plugin.save_resources)</td>
        <td></td>
    </tr></tbody>
</table>

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="api_version">
        <td>[`api_version`](#endstone.plugin.Plugin.api_version)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="authors">
        <td>[`authors`](#endstone.plugin.Plugin.authors)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="commands">
        <td>[`commands`](#endstone.plugin.Plugin.commands)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="config">
        <td>[`config`](#endstone.plugin.Plugin.config)</td>
        <td><code className="nextra-code">[dict](#dict)</code></td>
        <td></td>
    </tr>
    <tr key="contributors">
        <td>[`contributors`](#endstone.plugin.Plugin.contributors)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="default_permission">
        <td>[`default_permission`](#endstone.plugin.Plugin.default_permission)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="depend">
        <td>[`depend`](#endstone.plugin.Plugin.depend)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="description">
        <td>[`description`](#endstone.plugin.Plugin.description)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="load">
        <td>[`load`](#endstone.plugin.Plugin.load)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="load_before">
        <td>[`load_before`](#endstone.plugin.Plugin.load_before)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="name">
        <td>[`name`](#endstone.plugin.Plugin.name)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="permissions">
        <td>[`permissions`](#endstone.plugin.Plugin.permissions)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="prefix">
        <td>[`prefix`](#endstone.plugin.Plugin.prefix)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="provides">
        <td>[`provides`](#endstone.plugin.Plugin.provides)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="soft_depend">
        <td>[`soft_depend`](#endstone.plugin.Plugin.soft_depend)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="version">
        <td>[`version`](#endstone.plugin.Plugin.version)</td>
        <td></td>
        <td></td>
    </tr>
    <tr key="website">
        <td>[`website`](#endstone.plugin.Plugin.website)</td>
        <td></td>
        <td></td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> api_version [#endstone.plugin.Plugin.api_version]

```python
api_version = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> authors [#endstone.plugin.Plugin.authors]

```python
authors = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> commands [#endstone.plugin.Plugin.commands]

```python
commands = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> config [#endstone.plugin.Plugin.config]

```python
config: dict
```

#### <code className="doc-symbol doc-symbol-attribute"/> contributors [#endstone.plugin.Plugin.contributors]

```python
contributors = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> default_permission [#endstone.plugin.Plugin.default_permission]

```python
default_permission = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> depend [#endstone.plugin.Plugin.depend]

```python
depend = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> description [#endstone.plugin.Plugin.description]

```python
description = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> load [#endstone.plugin.Plugin.load]

```python
load = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> load_before [#endstone.plugin.Plugin.load_before]

```python
load_before = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> name [#endstone.plugin.Plugin.name]

```python
name = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> permissions [#endstone.plugin.Plugin.permissions]

```python
permissions = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> prefix [#endstone.plugin.Plugin.prefix]

```python
prefix = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> provides [#endstone.plugin.Plugin.provides]

```python
provides = None
```

#### <code className="doc-symbol doc-symbol-method"/> register_events [#endstone.plugin.Plugin.register_events]

```python
register_events(listener:object) -> None
```

#### <code className="doc-symbol doc-symbol-method"/> reload_config [#endstone.plugin.Plugin.reload_config]

```python
reload_config() -> dict
```

#### <code className="doc-symbol doc-symbol-method"/> save_config [#endstone.plugin.Plugin.save_config]

```python
save_config() -> None
```

#### <code className="doc-symbol doc-symbol-method"/> save_default_config [#endstone.plugin.Plugin.save_default_config]

```python
save_default_config() -> None
```

#### <code className="doc-symbol doc-symbol-method"/> save_resources [#endstone.plugin.Plugin.save_resources]

```python
save_resources(path:str, replace:bool = False) -> None
```

#### <code className="doc-symbol doc-symbol-attribute"/> soft_depend [#endstone.plugin.Plugin.soft_depend]

```python
soft_depend = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> version [#endstone.plugin.Plugin.version]

```python
version = None
```

#### <code className="doc-symbol doc-symbol-attribute"/> website [#endstone.plugin.Plugin.website]

```python
website = None
```

</div>

### <code className="doc-symbol doc-symbol-class"/> PluginCommand [#endstone.plugin.PluginCommand]

<div className="pl-[2em] mt-[1.25em]">

Bases: <code className="nextra-code">[Command](#endstone._internal.endstone_python.Command)</code>

Represents a Command belonging to a Plugin

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="executor">
        <td>[`executor`](#endstone.plugin.PluginCommand.executor)</td>
        <td><code className="nextra-code">[CommandExecutor](#endstone._internal.endstone_python.CommandExecutor)</code></td>
        <td>The CommandExecutor to run when parsing this command</td>
    </tr>
    <tr key="plugin">
        <td>[`plugin`](#endstone.plugin.PluginCommand.plugin)</td>
        <td><code className="nextra-code">[Plugin](#endstone._internal.endstone_python.Plugin)</code></td>
        <td>The owner of this PluginCommand</td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> executor [#endstone.plugin.PluginCommand.executor]

```python
executor: CommandExecutor
```

The CommandExecutor to run when parsing this command

#### <code className="doc-symbol doc-symbol-attribute"/> plugin [#endstone.plugin.PluginCommand.plugin]

```python
plugin: Plugin
```

The owner of this PluginCommand

</div>

### <code className="doc-symbol doc-symbol-class"/> PluginDescription [#endstone.plugin.PluginDescription]

```python
PluginDescription(
    name: str,
    version: str,
    description: str | None = None,
    load: PluginLoadOrder | None = None,
    authors: list[str] | None = None,
    contributors: list[str] | None = None,
    website: str | None = None,
    prefix: str | None = None,
    provides: list[str] | None = None,
    depend: list[str] | None = None,
    soft_depend: list[str] | None = None,
    load_before: list[str] | None = None,
    default_permission: PermissionDefault | None = None,
    commands: list[Command] | None = None,
    permissions: list[Permission] | None = None,
    *args: list[Permission] | None,
    **kwargs: list[Permission] | None
)
```

<div className="pl-[2em] mt-[1.25em]">

Represents the basic information about a plugin that the plugin loader needs to know.

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="api_version">
        <td>[`api_version`](#endstone.plugin.PluginDescription.api_version)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td>Gives the API version which this plugin is designed to support.</td>
    </tr>
    <tr key="authors">
        <td>[`authors`](#endstone.plugin.PluginDescription.authors)</td>
        <td><code className="nextra-code">[list](#list)[[str](#str)]</code></td>
        <td>Gives the list of authors for the plugin.</td>
    </tr>
    <tr key="commands">
        <td>[`commands`](#endstone.plugin.PluginDescription.commands)</td>
        <td><code className="nextra-code">[list](#list)[[Command](#endstone._internal.endstone_python.Command)]</code></td>
        <td>Gives the list of commands the plugin will register at runtime.</td>
    </tr>
    <tr key="contributors">
        <td>[`contributors`](#endstone.plugin.PluginDescription.contributors)</td>
        <td><code className="nextra-code">[list](#list)[[str](#str)]</code></td>
        <td>Gives the list of contributors for the plugin.</td>
    </tr>
    <tr key="default_permission">
        <td>[`default_permission`](#endstone.plugin.PluginDescription.default_permission)</td>
        <td><code className="nextra-code">[PermissionDefault](#endstone._internal.endstone_python.PermissionDefault)</code></td>
        <td>Gives the default value of permissions registered for the plugin.</td>
    </tr>
    <tr key="depend">
        <td>[`depend`](#endstone.plugin.PluginDescription.depend)</td>
        <td><code className="nextra-code">[list](#list)[[str](#str)]</code></td>
        <td>Gives a list of other plugins that the plugin requires.</td>
    </tr>
    <tr key="description">
        <td>[`description`](#endstone.plugin.PluginDescription.description)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td>Gives a human-friendly description of the functionality the plugin provides.</td>
    </tr>
    <tr key="full_name">
        <td>[`full_name`](#endstone.plugin.PluginDescription.full_name)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td>Returns the name of a plugin, including the version.</td>
    </tr>
    <tr key="load">
        <td>[`load`](#endstone.plugin.PluginDescription.load)</td>
        <td><code className="nextra-code">[PluginLoadOrder](#endstone._internal.endstone_python.PluginLoadOrder)</code></td>
        <td>Gives the phase of server startup that the plugin should be loaded.</td>
    </tr>
    <tr key="load_before">
        <td>[`load_before`](#endstone.plugin.PluginDescription.load_before)</td>
        <td><code className="nextra-code">[list](#list)[[str](#str)]</code></td>
        <td>Gets the list of plugins that should consider this plugin a soft-dependency.</td>
    </tr>
    <tr key="name">
        <td>[`name`](#endstone.plugin.PluginDescription.name)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td>Gives the name of the plugin. This name is a unique identifier for plugins.</td>
    </tr>
    <tr key="permissions">
        <td>[`permissions`](#endstone.plugin.PluginDescription.permissions)</td>
        <td><code className="nextra-code">[list](#list)[[Permission](#endstone._internal.endstone_python.Permission)]</code></td>
        <td>Gives the list of permissions the plugin will register at runtime, immediately proceeding enabling.</td>
    </tr>
    <tr key="prefix">
        <td>[`prefix`](#endstone.plugin.PluginDescription.prefix)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td>Gives the token to prefix plugin-specific logging messages with.</td>
    </tr>
    <tr key="provides">
        <td>[`provides`](#endstone.plugin.PluginDescription.provides)</td>
        <td><code className="nextra-code">[list](#list)[[str](#str)]</code></td>
        <td>Gives the list of other plugin APIs which this plugin provides. These are usable for other plugins to depend on.</td>
    </tr>
    <tr key="soft_depend">
        <td>[`soft_depend`](#endstone.plugin.PluginDescription.soft_depend)</td>
        <td><code className="nextra-code">[list](#list)[[str](#str)]</code></td>
        <td>Gives a list of other plugins that the plugin requires for full functionality.</td>
    </tr>
    <tr key="version">
        <td>[`version`](#endstone.plugin.PluginDescription.version)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td>Gives the version of the plugin.</td>
    </tr>
    <tr key="website">
        <td>[`website`](#endstone.plugin.PluginDescription.website)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td>Gives the plugin's or plugin's author's website.</td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> api_version [#endstone.plugin.PluginDescription.api_version]

```python
api_version: str
```

Gives the API version which this plugin is designed to support.

#### <code className="doc-symbol doc-symbol-attribute"/> authors [#endstone.plugin.PluginDescription.authors]

```python
authors: list[str]
```

Gives the list of authors for the plugin.

#### <code className="doc-symbol doc-symbol-attribute"/> commands [#endstone.plugin.PluginDescription.commands]

```python
commands: list[Command]
```

Gives the list of commands the plugin will register at runtime.

#### <code className="doc-symbol doc-symbol-attribute"/> contributors [#endstone.plugin.PluginDescription.contributors]

```python
contributors: list[str]
```

Gives the list of contributors for the plugin.

#### <code className="doc-symbol doc-symbol-attribute"/> default_permission [#endstone.plugin.PluginDescription.default_permission]

```python
default_permission: PermissionDefault
```

Gives the default value of permissions registered for the plugin.

#### <code className="doc-symbol doc-symbol-attribute"/> depend [#endstone.plugin.PluginDescription.depend]

```python
depend: list[str]
```

Gives a list of other plugins that the plugin requires.

#### <code className="doc-symbol doc-symbol-attribute"/> description [#endstone.plugin.PluginDescription.description]

```python
description: str
```

Gives a human-friendly description of the functionality the plugin provides.

#### <code className="doc-symbol doc-symbol-attribute"/> full_name [#endstone.plugin.PluginDescription.full_name]

```python
full_name: str
```

Returns the name of a plugin, including the version.

#### <code className="doc-symbol doc-symbol-attribute"/> load [#endstone.plugin.PluginDescription.load]

```python
load: PluginLoadOrder
```

Gives the phase of server startup that the plugin should be loaded.

#### <code className="doc-symbol doc-symbol-attribute"/> load_before [#endstone.plugin.PluginDescription.load_before]

```python
load_before: list[str]
```

Gets the list of plugins that should consider this plugin a soft-dependency.

#### <code className="doc-symbol doc-symbol-attribute"/> name [#endstone.plugin.PluginDescription.name]

```python
name: str
```

Gives the name of the plugin. This name is a unique identifier for plugins.

#### <code className="doc-symbol doc-symbol-attribute"/> permissions [#endstone.plugin.PluginDescription.permissions]

```python
permissions: list[Permission]
```

Gives the list of permissions the plugin will register at runtime, immediately proceeding enabling.

#### <code className="doc-symbol doc-symbol-attribute"/> prefix [#endstone.plugin.PluginDescription.prefix]

```python
prefix: str
```

Gives the token to prefix plugin-specific logging messages with.

#### <code className="doc-symbol doc-symbol-attribute"/> provides [#endstone.plugin.PluginDescription.provides]

```python
provides: list[str]
```

Gives the list of other plugin APIs which this plugin provides. These are usable for other plugins to depend on.

#### <code className="doc-symbol doc-symbol-attribute"/> soft_depend [#endstone.plugin.PluginDescription.soft_depend]

```python
soft_depend: list[str]
```

Gives a list of other plugins that the plugin requires for full functionality.

#### <code className="doc-symbol doc-symbol-attribute"/> version [#endstone.plugin.PluginDescription.version]

```python
version: str
```

Gives the version of the plugin.

#### <code className="doc-symbol doc-symbol-attribute"/> website [#endstone.plugin.PluginDescription.website]

```python
website: str
```

Gives the plugin's or plugin's author's website.

</div>

### <code className="doc-symbol doc-symbol-class"/> PluginLoadOrder [#endstone.plugin.PluginLoadOrder]

```python
PluginLoadOrder(value:int)
```

<div className="pl-[2em] mt-[1.25em]">

Represents the order in which a plugin should be initialized and enabled.

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="POSTWORLD">
        <td>[`POSTWORLD`](#endstone.plugin.PluginLoadOrder.POSTWORLD)</td>
        <td><code className="nextra-code">[PluginLoadOrder](#endstone._internal.endstone_python.PluginLoadOrder)</code></td>
        <td></td>
    </tr>
    <tr key="STARTUP">
        <td>[`STARTUP`](#endstone.plugin.PluginLoadOrder.STARTUP)</td>
        <td><code className="nextra-code">[PluginLoadOrder](#endstone._internal.endstone_python.PluginLoadOrder)</code></td>
        <td></td>
    </tr>
    <tr key="name">
        <td>[`name`](#endstone.plugin.PluginLoadOrder.name)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td></td>
    </tr>
    <tr key="value">
        <td>[`value`](#endstone.plugin.PluginLoadOrder.value)</td>
        <td><code className="nextra-code">[int](#int)</code></td>
        <td></td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> POSTWORLD [#endstone.plugin.PluginLoadOrder.POSTWORLD]

```python
POSTWORLD: PluginLoadOrder
```

#### <code className="doc-symbol doc-symbol-attribute"/> STARTUP [#endstone.plugin.PluginLoadOrder.STARTUP]

```python
STARTUP: PluginLoadOrder
```

#### <code className="doc-symbol doc-symbol-attribute"/> name [#endstone.plugin.PluginLoadOrder.name]

```python
name: str
```

#### <code className="doc-symbol doc-symbol-attribute"/> value [#endstone.plugin.PluginLoadOrder.value]

```python
value: int
```

</div>

### <code className="doc-symbol doc-symbol-class"/> PluginLoader [#endstone.plugin.PluginLoader]

```python
PluginLoader(server:Server)
```

<div className="pl-[2em] mt-[1.25em]">

Represents a plugin loader, which handles direct access to specific types of plugins

**Methods:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="disable_plugin">
        <td>[`disable_plugin`](#endstone.plugin.PluginLoader.disable_plugin)</td>
        <td>Disables the specified plugin</td>
    </tr>
    <tr key="enable_plugin">
        <td>[`enable_plugin`](#endstone.plugin.PluginLoader.enable_plugin)</td>
        <td>Enables the specified plugin</td>
    </tr>
    <tr key="load_plugin">
        <td>[`load_plugin`](#endstone.plugin.PluginLoader.load_plugin)</td>
        <td>Loads the plugin contained in the specified file</td>
    </tr>
    <tr key="load_plugins">
        <td>[`load_plugins`](#endstone.plugin.PluginLoader.load_plugins)</td>
        <td>Loads the plugin contained within the specified directory</td>
    </tr></tbody>
</table>

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="plugin_file_filters">
        <td>[`plugin_file_filters`](#endstone.plugin.PluginLoader.plugin_file_filters)</td>
        <td><code className="nextra-code">[list](#list)[[str](#str)]</code></td>
        <td>Returns a list of all filename filters expected by this PluginLoader</td>
    </tr>
    <tr key="server">
        <td>[`server`](#endstone.plugin.PluginLoader.server)</td>
        <td><code className="nextra-code">[Server](#endstone._internal.endstone_python.Server)</code></td>
        <td>Retrieves the Server object associated with the PluginLoader.</td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-method"/> disable_plugin [#endstone.plugin.PluginLoader.disable_plugin]

```python
disable_plugin(plugin:Plugin) -> None
```

Disables the specified plugin

#### <code className="doc-symbol doc-symbol-method"/> enable_plugin [#endstone.plugin.PluginLoader.enable_plugin]

```python
enable_plugin(plugin:Plugin) -> None
```

Enables the specified plugin

#### <code className="doc-symbol doc-symbol-method"/> load_plugin [#endstone.plugin.PluginLoader.load_plugin]

```python
load_plugin(file:str) -> Plugin
```

Loads the plugin contained in the specified file

#### <code className="doc-symbol doc-symbol-method"/> load_plugins [#endstone.plugin.PluginLoader.load_plugins]

```python
load_plugins(directory:str) -> list[Plugin]
```

Loads the plugin contained within the specified directory

#### <code className="doc-symbol doc-symbol-attribute"/> plugin_file_filters [#endstone.plugin.PluginLoader.plugin_file_filters]

```python
plugin_file_filters: list[str]
```

Returns a list of all filename filters expected by this PluginLoader

#### <code className="doc-symbol doc-symbol-attribute"/> server [#endstone.plugin.PluginLoader.server]

```python
server: Server
```

Retrieves the Server object associated with the PluginLoader.

</div>

### <code className="doc-symbol doc-symbol-class"/> PluginManager [#endstone.plugin.PluginManager]

<div className="pl-[2em] mt-[1.25em]">

Represents a plugin manager that handles all plugins from the Server

**Methods:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="call_event">
        <td>[`call_event`](#endstone.plugin.PluginManager.call_event)</td>
        <td>Calls an event which will be passed to plugins.</td>
    </tr>
    <tr key="clear_plugins">
        <td>[`clear_plugins`](#endstone.plugin.PluginManager.clear_plugins)</td>
        <td>Disables and removes all plugins</td>
    </tr>
    <tr key="disable_plugin">
        <td>[`disable_plugin`](#endstone.plugin.PluginManager.disable_plugin)</td>
        <td>Disables the specified plugin</td>
    </tr>
    <tr key="disable_plugins">
        <td>[`disable_plugins`](#endstone.plugin.PluginManager.disable_plugins)</td>
        <td>Disables all the loaded plugins</td>
    </tr>
    <tr key="enable_plugin">
        <td>[`enable_plugin`](#endstone.plugin.PluginManager.enable_plugin)</td>
        <td>Enables the specified plugin</td>
    </tr>
    <tr key="enable_plugins">
        <td>[`enable_plugins`](#endstone.plugin.PluginManager.enable_plugins)</td>
        <td>Enable all the loaded plugins</td>
    </tr>
    <tr key="get_default_perm_subscriptions">
        <td>[`get_default_perm_subscriptions`](#endstone.plugin.PluginManager.get_default_perm_subscriptions)</td>
        <td>Gets a set containing all subscribed Permissibles to the given default list, by permission level.</td>
    </tr>
    <tr key="get_default_permissions">
        <td>[`get_default_permissions`](#endstone.plugin.PluginManager.get_default_permissions)</td>
        <td>Gets the default permissions for the given permission level.</td>
    </tr>
    <tr key="get_permission">
        <td>[`get_permission`](#endstone.plugin.PluginManager.get_permission)</td>
        <td>Gets a Permission from its fully qualified name.</td>
    </tr>
    <tr key="get_permission_subscriptions">
        <td>[`get_permission_subscriptions`](#endstone.plugin.PluginManager.get_permission_subscriptions)</td>
        <td>Gets a set containing all subscribed Permissibles to the given permission.</td>
    </tr>
    <tr key="get_plugin">
        <td>[`get_plugin`](#endstone.plugin.PluginManager.get_plugin)</td>
        <td>Checks if the given plugin is loaded and returns it when applicable.</td>
    </tr>
    <tr key="is_plugin_enabled">
        <td>[`is_plugin_enabled`](#endstone.plugin.PluginManager.is_plugin_enabled)</td>
        <td></td>
    </tr>
    <tr key="load_plugin">
        <td>[`load_plugin`](#endstone.plugin.PluginManager.load_plugin)</td>
        <td>Loads the plugin in the specified file</td>
    </tr>
    <tr key="load_plugins">
        <td>[`load_plugins`](#endstone.plugin.PluginManager.load_plugins)</td>
        <td></td>
    </tr>
    <tr key="recalculate_permission_defaults">
        <td>[`recalculate_permission_defaults`](#endstone.plugin.PluginManager.recalculate_permission_defaults)</td>
        <td>Recalculates the defaults for the given Permission.</td>
    </tr>
    <tr key="register_event">
        <td>[`register_event`](#endstone.plugin.PluginManager.register_event)</td>
        <td>Registers the given event</td>
    </tr>
    <tr key="remove_permission">
        <td>[`remove_permission`](#endstone.plugin.PluginManager.remove_permission)</td>
        <td></td>
    </tr>
    <tr key="subscribe_to_default_perms">
        <td>[`subscribe_to_default_perms`](#endstone.plugin.PluginManager.subscribe_to_default_perms)</td>
        <td>Subscribes to the given Default permissions by permission level.</td>
    </tr>
    <tr key="subscribe_to_permission">
        <td>[`subscribe_to_permission`](#endstone.plugin.PluginManager.subscribe_to_permission)</td>
        <td>Subscribes the given Permissible for information about the requested Permission.</td>
    </tr>
    <tr key="unsubscribe_from_default_perms">
        <td>[`unsubscribe_from_default_perms`](#endstone.plugin.PluginManager.unsubscribe_from_default_perms)</td>
        <td>Unsubscribes from the given Default permissions by permission level.</td>
    </tr>
    <tr key="unsubscribe_from_permission">
        <td>[`unsubscribe_from_permission`](#endstone.plugin.PluginManager.unsubscribe_from_permission)</td>
        <td>Unsubscribes the given Permissible for information about the requested Permission.</td>
    </tr></tbody>
</table>

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="permissions">
        <td>[`permissions`](#endstone.plugin.PluginManager.permissions)</td>
        <td><code className="nextra-code">[set](#set)[[Permission](#endstone._internal.endstone_python.Permission)]</code></td>
        <td>Gets a set of all registered permissions.</td>
    </tr>
    <tr key="plugins">
        <td>[`plugins`](#endstone.plugin.PluginManager.plugins)</td>
        <td><code className="nextra-code">[list](#list)[[Plugin](#endstone._internal.endstone_python.Plugin)]</code></td>
        <td>Gets a list of all currently loaded plugins</td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-method"/> call_event [#endstone.plugin.PluginManager.call_event]

```python
call_event(event:Event) -> None
```

Calls an event which will be passed to plugins.

#### <code className="doc-symbol doc-symbol-method"/> clear_plugins [#endstone.plugin.PluginManager.clear_plugins]

```python
clear_plugins() -> None
```

Disables and removes all plugins

#### <code className="doc-symbol doc-symbol-method"/> disable_plugin [#endstone.plugin.PluginManager.disable_plugin]

```python
disable_plugin(plugin:Plugin) -> None
```

Disables the specified plugin

#### <code className="doc-symbol doc-symbol-method"/> disable_plugins [#endstone.plugin.PluginManager.disable_plugins]

```python
disable_plugins() -> None
```

Disables all the loaded plugins

#### <code className="doc-symbol doc-symbol-method"/> enable_plugin [#endstone.plugin.PluginManager.enable_plugin]

```python
enable_plugin(plugin:Plugin) -> None
```

Enables the specified plugin

#### <code className="doc-symbol doc-symbol-method"/> enable_plugins [#endstone.plugin.PluginManager.enable_plugins]

```python
enable_plugins() -> None
```

Enable all the loaded plugins

#### <code className="doc-symbol doc-symbol-method"/> get_default_perm_subscriptions [#endstone.plugin.PluginManager.get_default_perm_subscriptions]

```python
get_default_perm_subscriptions(level:PermissionLevel) -> set[Permissible]
```

Gets a set containing all subscribed Permissibles to the given default list, by permission level.

#### <code className="doc-symbol doc-symbol-method"/> get_default_permissions [#endstone.plugin.PluginManager.get_default_permissions]

```python
get_default_permissions(level:PermissionLevel) -> list[Permission]
```

Gets the default permissions for the given permission level.

#### <code className="doc-symbol doc-symbol-method"/> get_permission [#endstone.plugin.PluginManager.get_permission]

```python
get_permission(name:str) -> Permission
```

Gets a Permission from its fully qualified name.

#### <code className="doc-symbol doc-symbol-method"/> get_permission_subscriptions [#endstone.plugin.PluginManager.get_permission_subscriptions]

```python
get_permission_subscriptions(permission:str) -> set[Permissible]
```

Gets a set containing all subscribed Permissibles to the given permission.

#### <code className="doc-symbol doc-symbol-method"/> get_plugin [#endstone.plugin.PluginManager.get_plugin]

```python
get_plugin(name:str) -> Plugin
```

Checks if the given plugin is loaded and returns it when applicable.

#### <code className="doc-symbol doc-symbol-method"/> is_plugin_enabled [#endstone.plugin.PluginManager.is_plugin_enabled]

```python
is_plugin_enabled()
```

#### <code className="doc-symbol doc-symbol-method"/> load_plugin [#endstone.plugin.PluginManager.load_plugin]

```python
load_plugin(file:str) -> Plugin
```

Loads the plugin in the specified file

#### <code className="doc-symbol doc-symbol-method"/> load_plugins [#endstone.plugin.PluginManager.load_plugins]

```python
load_plugins()
```

#### <code className="doc-symbol doc-symbol-attribute"/> permissions [#endstone.plugin.PluginManager.permissions]

```python
permissions: set[Permission]
```

Gets a set of all registered permissions.

#### <code className="doc-symbol doc-symbol-attribute"/> plugins [#endstone.plugin.PluginManager.plugins]

```python
plugins: list[Plugin]
```

Gets a list of all currently loaded plugins

#### <code className="doc-symbol doc-symbol-method"/> recalculate_permission_defaults [#endstone.plugin.PluginManager.recalculate_permission_defaults]

```python
recalculate_permission_defaults(perm:Permission) -> None
```

Recalculates the defaults for the given Permission.

#### <code className="doc-symbol doc-symbol-method"/> register_event [#endstone.plugin.PluginManager.register_event]

```python
register_event(
    name: str,
    executor: Callable[[Event], None],
    priority: EventPriority,
    plugin: Plugin,
    ignore_cancelled: bool,
) -> None
```

Registers the given event

#### <code className="doc-symbol doc-symbol-method"/> remove_permission [#endstone.plugin.PluginManager.remove_permission]

```python
remove_permission()
```

#### <code className="doc-symbol doc-symbol-method"/> subscribe_to_default_perms [#endstone.plugin.PluginManager.subscribe_to_default_perms]

```python
subscribe_to_default_perms(
    level: PermissionLevel, permissible: Permissible
) -> None
```

Subscribes to the given Default permissions by permission level.

#### <code className="doc-symbol doc-symbol-method"/> subscribe_to_permission [#endstone.plugin.PluginManager.subscribe_to_permission]

```python
subscribe_to_permission(permission:str, permissible:Permissible) -> None
```

Subscribes the given Permissible for information about the requested Permission.

#### <code className="doc-symbol doc-symbol-method"/> unsubscribe_from_default_perms [#endstone.plugin.PluginManager.unsubscribe_from_default_perms]

```python
unsubscribe_from_default_perms(
    level: PermissionLevel, permissible: Permissible
) -> None
```

Unsubscribes from the given Default permissions by permission level.

#### <code className="doc-symbol doc-symbol-method"/> unsubscribe_from_permission [#endstone.plugin.PluginManager.unsubscribe_from_permission]

```python
unsubscribe_from_permission(permission:str, permissible:Permissible) -> None
```

Unsubscribes the given Permissible for information about the requested Permission.

</div>

### <code className="doc-symbol doc-symbol-class"/> Service [#endstone.plugin.Service]

```python
Service()
```

<div className="pl-[2em] mt-[1.25em]">

Represents a list of methods.

</div>

### <code className="doc-symbol doc-symbol-class"/> ServiceManager [#endstone.plugin.ServiceManager]

<div className="pl-[2em] mt-[1.25em]">

Represent a service manager that manages services and service providers.

**Methods:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="load">
        <td>[`load`](#endstone.plugin.ServiceManager.load)</td>
        <td></td>
    </tr>
    <tr key="register">
        <td>[`register`](#endstone.plugin.ServiceManager.register)</td>
        <td>Register a provider of a service.</td>
    </tr>
    <tr key="unregister">
        <td>[`unregister`](#endstone.plugin.ServiceManager.unregister)</td>
        <td></td>
    </tr>
    <tr key="unregister_all">
        <td>[`unregister_all`](#endstone.plugin.ServiceManager.unregister_all)</td>
        <td>Unregister all the services registered by a particular plugin.</td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-method"/> load [#endstone.plugin.ServiceManager.load]

```python
load(name:str) -> Service
```

#### <code className="doc-symbol doc-symbol-method"/> register [#endstone.plugin.ServiceManager.register]

```python
register(
    name: str, provider: Service, plugin: Plugin, priority: ServicePriority
) -> None
```

Register a provider of a service.

#### <code className="doc-symbol doc-symbol-method"/> unregister [#endstone.plugin.ServiceManager.unregister]

```python
unregister()
```

#### <code className="doc-symbol doc-symbol-method"/> unregister_all [#endstone.plugin.ServiceManager.unregister_all]

```python
unregister_all(plugin:Plugin) -> None
```

Unregister all the services registered by a particular plugin.

</div>

### <code className="doc-symbol doc-symbol-class"/> ServicePriority [#endstone.plugin.ServicePriority]

```python
ServicePriority(value:int)
```

<div className="pl-[2em] mt-[1.25em]">

Represents various priorities of a provider.

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="HIGH">
        <td>[`HIGH`](#endstone.plugin.ServicePriority.HIGH)</td>
        <td><code className="nextra-code">[ServicePriority](#endstone._internal.endstone_python.ServicePriority)</code></td>
        <td></td>
    </tr>
    <tr key="HIGHEST">
        <td>[`HIGHEST`](#endstone.plugin.ServicePriority.HIGHEST)</td>
        <td><code className="nextra-code">[ServicePriority](#endstone._internal.endstone_python.ServicePriority)</code></td>
        <td></td>
    </tr>
    <tr key="LOW">
        <td>[`LOW`](#endstone.plugin.ServicePriority.LOW)</td>
        <td><code className="nextra-code">[ServicePriority](#endstone._internal.endstone_python.ServicePriority)</code></td>
        <td></td>
    </tr>
    <tr key="LOWEST">
        <td>[`LOWEST`](#endstone.plugin.ServicePriority.LOWEST)</td>
        <td><code className="nextra-code">[ServicePriority](#endstone._internal.endstone_python.ServicePriority)</code></td>
        <td></td>
    </tr>
    <tr key="NORMAL">
        <td>[`NORMAL`](#endstone.plugin.ServicePriority.NORMAL)</td>
        <td><code className="nextra-code">[ServicePriority](#endstone._internal.endstone_python.ServicePriority)</code></td>
        <td></td>
    </tr>
    <tr key="name">
        <td>[`name`](#endstone.plugin.ServicePriority.name)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td></td>
    </tr>
    <tr key="value">
        <td>[`value`](#endstone.plugin.ServicePriority.value)</td>
        <td><code className="nextra-code">[int](#int)</code></td>
        <td></td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> HIGH [#endstone.plugin.ServicePriority.HIGH]

```python
HIGH: ServicePriority
```

#### <code className="doc-symbol doc-symbol-attribute"/> HIGHEST [#endstone.plugin.ServicePriority.HIGHEST]

```python
HIGHEST: ServicePriority
```

#### <code className="doc-symbol doc-symbol-attribute"/> LOW [#endstone.plugin.ServicePriority.LOW]

```python
LOW: ServicePriority
```

#### <code className="doc-symbol doc-symbol-attribute"/> LOWEST [#endstone.plugin.ServicePriority.LOWEST]

```python
LOWEST: ServicePriority
```

#### <code className="doc-symbol doc-symbol-attribute"/> NORMAL [#endstone.plugin.ServicePriority.NORMAL]

```python
NORMAL: ServicePriority
```

#### <code className="doc-symbol doc-symbol-attribute"/> name [#endstone.plugin.ServicePriority.name]

```python
name: str
```

#### <code className="doc-symbol doc-symbol-attribute"/> value [#endstone.plugin.ServicePriority.value]

```python
value: int
```

</div>
