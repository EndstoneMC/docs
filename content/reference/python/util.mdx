---
title: "endstone.util"
---

## <code className="doc-symbol doc-symbol-module"/> endstone.util [#endstone.util]

**Classes:**

Name | Description
---- | -----------
[`SocketAddress`](#endstone.util.SocketAddress) | Represents an IP Socket Address (hostname + port number).
[`Vector`](#endstone.util.Vector) | Represents a 3-dimensional vector.

### <code className="doc-symbol doc-symbol-class"/> SocketAddress [#endstone.util.SocketAddress]

```python
SocketAddress()
```

<div className="pl-[2em] mt-[1.25em]">

Represents an IP Socket Address (hostname + port number).

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="hostname">
        <td>[`hostname`](#endstone.util.SocketAddress.hostname)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td>Gets the hostname.</td>
    </tr>
    <tr key="port">
        <td>[`port`](#endstone.util.SocketAddress.port)</td>
        <td><code className="nextra-code">[int](#int)</code></td>
        <td>Gets the port number.</td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> hostname [#endstone.util.SocketAddress.hostname]

```python
hostname: str
```

Gets the hostname.

#### <code className="doc-symbol doc-symbol-attribute"/> port [#endstone.util.SocketAddress.port]

```python
port: int
```

Gets the port number.

</div>

### <code className="doc-symbol doc-symbol-class"/> Vector [#endstone.util.Vector]

```python
Vector()
```

<div className="pl-[2em] mt-[1.25em]">

Represents a 3-dimensional vector.

**Methods:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="distance">
        <td>[`distance`](#endstone.util.Vector.distance)</td>
        <td>The distance between this Vector and another</td>
    </tr>
    <tr key="distance_squared">
        <td>[`distance_squared`](#endstone.util.Vector.distance_squared)</td>
        <td>The squared distance between this Vector and another</td>
    </tr></tbody>
</table>

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="length">
        <td>[`length`](#endstone.util.Vector.length)</td>
        <td><code className="nextra-code">[float](#float)</code></td>
        <td>The magnitude of the Vector</td>
    </tr>
    <tr key="length_squared">
        <td>[`length_squared`](#endstone.util.Vector.length_squared)</td>
        <td><code className="nextra-code">[float](#float)</code></td>
        <td>The squared magnitude of the Vector</td>
    </tr>
    <tr key="x">
        <td>[`x`](#endstone.util.Vector.x)</td>
        <td><code className="nextra-code">[float](#float)</code></td>
        <td>The X component of the vector</td>
    </tr>
    <tr key="y">
        <td>[`y`](#endstone.util.Vector.y)</td>
        <td><code className="nextra-code">[float](#float)</code></td>
        <td>The Y component of the vector</td>
    </tr>
    <tr key="z">
        <td>[`z`](#endstone.util.Vector.z)</td>
        <td><code className="nextra-code">[float](#float)</code></td>
        <td>The Z component of the vector</td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> length [#endstone.util.Vector.length]

```python
length: float
```

The magnitude of the Vector

#### <code className="doc-symbol doc-symbol-attribute"/> length_squared [#endstone.util.Vector.length_squared]

```python
length_squared: float
```

The squared magnitude of the Vector

#### <code className="doc-symbol doc-symbol-attribute"/> x [#endstone.util.Vector.x]

```python
x: float
```

The X component of the vector

#### <code className="doc-symbol doc-symbol-attribute"/> y [#endstone.util.Vector.y]

```python
y: float
```

The Y component of the vector

#### <code className="doc-symbol doc-symbol-attribute"/> z [#endstone.util.Vector.z]

```python
z: float
```

The Z component of the vector

#### <code className="doc-symbol doc-symbol-method"/> distance [#endstone.util.Vector.distance]

```python
distance(other:Vector) -> float
```

The distance between this Vector and another

#### <code className="doc-symbol doc-symbol-method"/> distance_squared [#endstone.util.Vector.distance_squared]

```python
distance_squared(other:Vector) -> float
```

The squared distance between this Vector and another

</div>
