---
title: "endstone.map"
---

## <code className="doc-symbol doc-symbol-module"/> endstone.map [#endstone.map]

**Classes:**

Name | Description
---- | -----------
[`MapCanvas`](#endstone.map.MapCanvas) | Represents a canvas for drawing to a map. Each canvas is associated with a specific MapRenderer and represents that renderer's layer on the map.
[`MapRenderer`](#endstone.map.MapRenderer) | Represents a renderer for a map.
[`MapView`](#endstone.map.MapView) | Represents a map item.

### <code className="doc-symbol doc-symbol-class"/> MapCanvas [#endstone.map.MapCanvas]

<div className="pl-[2em] mt-[1.25em]">

Represents a canvas for drawing to a map. Each canvas is associated with a specific MapRenderer and represents that renderer's layer on the map.

**Methods:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="draw_image">
        <td>[`draw_image`](#endstone.map.MapCanvas.draw_image)</td>
        <td>Draw an image to the map. The image will be clipped if necessary.</td>
    </tr>
    <tr key="get_base_pixel_color">
        <td>[`get_base_pixel_color`](#endstone.map.MapCanvas.get_base_pixel_color)</td>
        <td>Get a pixel from the layers below this canvas.</td>
    </tr>
    <tr key="get_pixel_color">
        <td>[`get_pixel_color`](#endstone.map.MapCanvas.get_pixel_color)</td>
        <td>Get a pixel from the canvas.</td>
    </tr>
    <tr key="set_pixel_color">
        <td>[`set_pixel_color`](#endstone.map.MapCanvas.set_pixel_color)</td>
        <td>Draw a pixel to the canvas.</td>
    </tr></tbody>
</table>

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="map_view">
        <td>[`map_view`](#endstone.map.MapCanvas.map_view)</td>
        <td><code className="nextra-code">[MapView](#endstone._internal.endstone_python.MapView)</code></td>
        <td>Get the map this canvas is attached to.</td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> map_view [#endstone.map.MapCanvas.map_view]

```python
map_view: MapView
```

Get the map this canvas is attached to.

#### <code className="doc-symbol doc-symbol-method"/> draw_image [#endstone.map.MapCanvas.draw_image]

```python
draw_image(x:int, y:int, image:ndarray[uint8]) -> None
```

Draw an image to the map. The image will be clipped if necessary.

#### <code className="doc-symbol doc-symbol-method"/> get_base_pixel_color [#endstone.map.MapCanvas.get_base_pixel_color]

```python
get_base_pixel_color(x:int, y:int) -> tuple[int, ...]
```

Get a pixel from the layers below this canvas.

#### <code className="doc-symbol doc-symbol-method"/> get_pixel_color [#endstone.map.MapCanvas.get_pixel_color]

```python
get_pixel_color(x:int, y:int) -> tuple[int, ...] | None
```

Get a pixel from the canvas.

If no color is set at the given position for this canvas, then None is returned and the color returned by get_base_pixel_color() is shown on the map

#### <code className="doc-symbol doc-symbol-method"/> set_pixel_color [#endstone.map.MapCanvas.set_pixel_color]

```python
set_pixel_color(x:int, y:int, color:tuple[int, ...] | None) -> None
```

Draw a pixel to the canvas.

If None is used as color, then the color returned by get_base_pixel_color() is shown on the map.

</div>

### <code className="doc-symbol doc-symbol-class"/> MapRenderer [#endstone.map.MapRenderer]

```python
MapRenderer(is_contextual:bool = False)
```

<div className="pl-[2em] mt-[1.25em]">

Represents a renderer for a map.

**Methods:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="initialize">
        <td>[`initialize`](#endstone.map.MapRenderer.initialize)</td>
        <td>Initialize this MapRenderer for the given map.</td>
    </tr>
    <tr key="render">
        <td>[`render`](#endstone.map.MapRenderer.render)</td>
        <td>Render to the given map.</td>
    </tr></tbody>
</table>

Initialize the map renderer base with the given contextual status.

#### <code className="doc-symbol doc-symbol-method"/> initialize [#endstone.map.MapRenderer.initialize]

```python
initialize(view:MapView) -> None
```

Initialize this MapRenderer for the given map.

#### <code className="doc-symbol doc-symbol-method"/> render [#endstone.map.MapRenderer.render]

```python
render(map:MapView, canvas:MapCanvas, player:Player) -> None
```

Render to the given map.

</div>

### <code className="doc-symbol doc-symbol-class"/> MapView [#endstone.map.MapView]

<div className="pl-[2em] mt-[1.25em]">

Represents a map item.

**Classes:**

Name | Description
---- | -----------
[`Scale`](#endstone.map.MapView.Scale) | An enum representing all possible scales a map can be set to.

**Methods:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="add_renderer">
        <td>[`add_renderer`](#endstone.map.MapView.add_renderer)</td>
        <td>Add a renderer to this map.</td>
    </tr>
    <tr key="remove_renderer">
        <td>[`remove_renderer`](#endstone.map.MapView.remove_renderer)</td>
        <td>Remove a renderer from this map.</td>
    </tr></tbody>
</table>

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="center_x">
        <td>[`center_x`](#endstone.map.MapView.center_x)</td>
        <td><code className="nextra-code">[int](#int)</code></td>
        <td>Get or set the center X position of this map.</td>
    </tr>
    <tr key="center_z">
        <td>[`center_z`](#endstone.map.MapView.center_z)</td>
        <td><code className="nextra-code">[int](#int)</code></td>
        <td>Get or set the center Z position of this map.</td>
    </tr>
    <tr key="dimension">
        <td>[`dimension`](#endstone.map.MapView.dimension)</td>
        <td><code className="nextra-code">[Dimension](#endstone._internal.endstone_python.Dimension)</code></td>
        <td>Get or set the dimension that this map is associated with.</td>
    </tr>
    <tr key="id">
        <td>[`id`](#endstone.map.MapView.id)</td>
        <td><code className="nextra-code">[int](#int)</code></td>
        <td>Get the ID of this map item for use with MapMeta.</td>
    </tr>
    <tr key="is_unlimited_tracking">
        <td>[`is_unlimited_tracking`](#endstone.map.MapView.is_unlimited_tracking)</td>
        <td><code className="nextra-code">[bool](#bool)</code></td>
        <td>Whether the map will show a smaller position cursor (true), or no position cursor (false) when cursor is outside of map's range.</td>
    </tr>
    <tr key="is_virtual">
        <td>[`is_virtual`](#endstone.map.MapView.is_virtual)</td>
        <td><code className="nextra-code">[bool](#bool)</code></td>
        <td>Check whether this map is virtual.</td>
    </tr>
    <tr key="locked">
        <td>[`locked`](#endstone.map.MapView.locked)</td>
        <td><code className="nextra-code">[bool](#bool)</code></td>
        <td>Whether the map is locked or not. A locked map may not be explored further.</td>
    </tr>
    <tr key="renderers">
        <td>[`renderers`](#endstone.map.MapView.renderers)</td>
        <td><code className="nextra-code">[list](#list)[[MapRenderer](#endstone._internal.endstone_python.MapRenderer)]</code></td>
        <td>Get a copied list of MapRenderers currently in effect.</td>
    </tr>
    <tr key="scale">
        <td>[`scale`](#endstone.map.MapView.scale)</td>
        <td><code className="nextra-code">[Scale](#endstone._internal.endstone_python.MapView.Scale)</code></td>
        <td>Get or set the scale of this map.</td>
    </tr></tbody>
</table>

#### <code className="doc-symbol doc-symbol-attribute"/> center_x [#endstone.map.MapView.center_x]

```python
center_x: int
```

Get or set the center X position of this map.

#### <code className="doc-symbol doc-symbol-attribute"/> center_z [#endstone.map.MapView.center_z]

```python
center_z: int
```

Get or set the center Z position of this map.

#### <code className="doc-symbol doc-symbol-attribute"/> dimension [#endstone.map.MapView.dimension]

```python
dimension: Dimension
```

Get or set the dimension that this map is associated with.

#### <code className="doc-symbol doc-symbol-attribute"/> id [#endstone.map.MapView.id]

```python
id: int
```

Get the ID of this map item for use with MapMeta.

#### <code className="doc-symbol doc-symbol-attribute"/> is_unlimited_tracking [#endstone.map.MapView.is_unlimited_tracking]

```python
is_unlimited_tracking: bool
```

Whether the map will show a smaller position cursor (true), or no position cursor (false) when cursor is outside of map's range.

#### <code className="doc-symbol doc-symbol-attribute"/> is_virtual [#endstone.map.MapView.is_virtual]

```python
is_virtual: bool
```

Check whether this map is virtual.

#### <code className="doc-symbol doc-symbol-attribute"/> locked [#endstone.map.MapView.locked]

```python
locked: bool
```

Whether the map is locked or not. A locked map may not be explored further.

#### <code className="doc-symbol doc-symbol-attribute"/> renderers [#endstone.map.MapView.renderers]

```python
renderers: list[MapRenderer]
```

Get a copied list of MapRenderers currently in effect.

#### <code className="doc-symbol doc-symbol-attribute"/> scale [#endstone.map.MapView.scale]

```python
scale: MapView.Scale
```

Get or set the scale of this map.

#### <code className="doc-symbol doc-symbol-class"/> Scale [#endstone.map.MapView.Scale]

```python
Scale(value:int)
```

<div className="pl-[2em] mt-[1.25em]">

An enum representing all possible scales a map can be set to.

**Attributes:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
    <tr key="CLOSE">
        <td>[`CLOSE`](#endstone.map.MapView.Scale.CLOSE)</td>
        <td><code className="nextra-code">[Scale](#endstone._internal.endstone_python.MapView.Scale)</code></td>
        <td></td>
    </tr>
    <tr key="CLOSEST">
        <td>[`CLOSEST`](#endstone.map.MapView.Scale.CLOSEST)</td>
        <td><code className="nextra-code">[Scale](#endstone._internal.endstone_python.MapView.Scale)</code></td>
        <td></td>
    </tr>
    <tr key="FAR">
        <td>[`FAR`](#endstone.map.MapView.Scale.FAR)</td>
        <td><code className="nextra-code">[Scale](#endstone._internal.endstone_python.MapView.Scale)</code></td>
        <td></td>
    </tr>
    <tr key="FARTHEST">
        <td>[`FARTHEST`](#endstone.map.MapView.Scale.FARTHEST)</td>
        <td><code className="nextra-code">[Scale](#endstone._internal.endstone_python.MapView.Scale)</code></td>
        <td></td>
    </tr>
    <tr key="NORMAL">
        <td>[`NORMAL`](#endstone.map.MapView.Scale.NORMAL)</td>
        <td><code className="nextra-code">[Scale](#endstone._internal.endstone_python.MapView.Scale)</code></td>
        <td></td>
    </tr>
    <tr key="name">
        <td>[`name`](#endstone.map.MapView.Scale.name)</td>
        <td><code className="nextra-code">[str](#str)</code></td>
        <td></td>
    </tr>
    <tr key="value">
        <td>[`value`](#endstone.map.MapView.Scale.value)</td>
        <td><code className="nextra-code">[int](#int)</code></td>
        <td></td>
    </tr></tbody>
</table>

##### <code className="doc-symbol doc-symbol-attribute"/> CLOSE [#endstone.map.MapView.Scale.CLOSE]

```python
CLOSE: MapView.Scale
```

##### <code className="doc-symbol doc-symbol-attribute"/> CLOSEST [#endstone.map.MapView.Scale.CLOSEST]

```python
CLOSEST: MapView.Scale
```

##### <code className="doc-symbol doc-symbol-attribute"/> FAR [#endstone.map.MapView.Scale.FAR]

```python
FAR: MapView.Scale
```

##### <code className="doc-symbol doc-symbol-attribute"/> FARTHEST [#endstone.map.MapView.Scale.FARTHEST]

```python
FARTHEST: MapView.Scale
```

##### <code className="doc-symbol doc-symbol-attribute"/> NORMAL [#endstone.map.MapView.Scale.NORMAL]

```python
NORMAL: MapView.Scale
```

##### <code className="doc-symbol doc-symbol-attribute"/> name [#endstone.map.MapView.Scale.name]

```python
name: str
```

##### <code className="doc-symbol doc-symbol-attribute"/> value [#endstone.map.MapView.Scale.value]

```python
value: int
```

</div>

#### <code className="doc-symbol doc-symbol-method"/> add_renderer [#endstone.map.MapView.add_renderer]

```python
add_renderer(renderer:MapRenderer) -> None
```

Add a renderer to this map.

#### <code className="doc-symbol doc-symbol-method"/> remove_renderer [#endstone.map.MapView.remove_renderer]

```python
remove_renderer(renderer:MapRenderer) -> bool
```

Remove a renderer from this map.

</div>
