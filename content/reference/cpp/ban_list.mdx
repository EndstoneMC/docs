---
title: "BanList"
---

## <code className="doc-symbol doc-symbol-class"/> endstone::BanList

**template &lt;typename T&gt;**
















**Functions:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
<tr key="classendstone_1_1_ban_list_1a61b4780100f97cc243ccc9195c556959">
    <td>[`addBan`](#function-addban-12)</td>
    <td>Adds a ban to this list. If a previous ban exists, this will update the previous entry.</td>
</tr>
<tr key="classendstone_1_1_ban_list_1a341a701b475641623b2ce42f7a8c8f73">
    <td>[`addBan`](#function-addban-22)</td>
    <td>Adds a ban to this list. If a previous ban exists, this will update the previous entry.</td>
</tr>
<tr key="classendstone_1_1_ban_list_1acbf544a1e7d2a8950b23668a6bc28488">
    <td>[`getBanEntry`](#function-getbanentry-12)</td>
    <td>Gets a BanEntry by target.</td>
</tr>
<tr key="classendstone_1_1_ban_list_1a6bf71e53fc104040604afe2de4cea0c3">
    <td>[`getBanEntry`](#function-getbanentry-22)</td>
    <td>Gets a BanEntry by target.</td>
</tr>
<tr key="classendstone_1_1_ban_list_1aad4b42721be143342e22d1ba2fa325e2">
    <td>[`getEntries`](#function-getentries-12)</td>
    <td>Gets a vector containing pointers to every BanEntry in this list.</td>
</tr>
<tr key="classendstone_1_1_ban_list_1afcd17dd71093a6ad37ae785797e09511">
    <td>[`getEntries`](#function-getentries-22)</td>
    <td>Gets a vector containing pointers to every BanEntry in this list.</td>
</tr>
<tr key="classendstone_1_1_ban_list_1a7fd3e0a897b072736e9df35d91b12c19">
    <td>[`isBanned`](#function-isbanned)</td>
    <td>Checks if a BanEntry exists for the target, indicating an active ban status.</td>
</tr>
<tr key="classendstone_1_1_ban_list_1abefb6ec9f4a696e98609579e3b87718a">
    <td>[`removeBan`](#function-removeban)</td>
    <td>Removes the specified target from this list, therefore indicating a "not banned" status.</td>
</tr>
<tr key="classendstone_1_1_ban_list_1a409cf606a2985931ed9a7eb51b9362ea">
    <td>[`~BanList`](#function-banlist)</td>
    <td></td>
</tr>
</tbody>
</table>

### <code className="doc-symbol doc-symbol-function"/> addBan

```cpp
virtual T & endstone::BanList::addBan (
    std::string target,
    std::optional< std::string > reason,
    std::optional< BanEntry::Date > expires,
    std::optional< std::string > source
) = 0
```

Adds a ban to this list. If a previous ban exists, this will update the previous entry.



**Parameters:**


* `target` The target of the ban. 
* `reason` The reason for the ban, std::nullopt indicates implementation default. 
* `expires` The date for the ban's expiration (unban), or std::nullopt to imply forever. 
* `source` The source of the ban, std::nullopt indicates implementation default. 



**Returns:**

T& The entry for the newly created ban, or the entry for the (updated) previous ban. 





        
### <code className="doc-symbol doc-symbol-function"/> addBan

```cpp
virtual T & endstone::BanList::addBan (
    std::string target,
    std::optional< std::string > reason,
    std::chrono::seconds duration,
    std::optional< std::string > source
) = 0
```

Adds a ban to this list. If a previous ban exists, this will update the previous entry.



**Parameters:**


* `target` The target of the ban. 
* `reason` The reason for the ban, std::nullopt indicates implementation default. 
* `duration` The duration of the ban 
* `source` The source of the ban, std::nullopt indicates implementation default. 



**Returns:**

T& The entry for the newly created ban, or the entry for the (updated) previous ban. 





        
### <code className="doc-symbol doc-symbol-function"/> getBanEntry

```cpp
virtual const T * endstone::BanList::getBanEntry (
    std::string target
) const = 0
```

Gets a BanEntry by target.



**Parameters:**


* `target` The entry parameter to search for. 



**Returns:**

T\* The corresponding entry, or nullptr if none found. 





        
### <code className="doc-symbol doc-symbol-function"/> getBanEntry

```cpp
virtual T * endstone::BanList::getBanEntry (
    std::string target
) = 0
```

Gets a BanEntry by target.



**Parameters:**


* `target` The entry parameter to search for. 



**Returns:**

T\* The corresponding entry, or nullptr if none found. 





        
### <code className="doc-symbol doc-symbol-function"/> getEntries

```cpp
virtual std::vector< const T * > endstone::BanList::getEntries () const = 0
```

Gets a vector containing pointers to every BanEntry in this list.



**Returns:**

A vector containing pointers to every entry tracked by this list. 





        
### <code className="doc-symbol doc-symbol-function"/> getEntries

```cpp
virtual std::vector< T * > endstone::BanList::getEntries () = 0
```

Gets a vector containing pointers to every BanEntry in this list.



**Returns:**

A vector containing pointers to every entry tracked by this list. 





        
### <code className="doc-symbol doc-symbol-function"/> isBanned

```cpp
virtual bool endstone::BanList::isBanned (
    std::string target
) const = 0
```

Checks if a BanEntry exists for the target, indicating an active ban status.



**Parameters:**


* `target` The target to find. 



**Returns:**

true If a [**BanEntry**](classendstone_1_1_ban_entry.md) exists for the target, indicating an active ban status. 




**Returns:**

false Otherwise. 





        
### <code className="doc-symbol doc-symbol-function"/> removeBan

```cpp
virtual void endstone::BanList::removeBan (
    std::string target
) = 0
```

Removes the specified target from this list, therefore indicating a "not banned" status.



**Parameters:**


* `target` The target to remove from this list. 




        