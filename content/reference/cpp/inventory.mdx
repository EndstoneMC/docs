---
title: "Inventory"
---

## <code className="doc-symbol doc-symbol-class"/> endstone::Inventory

















**Functions:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
<tr key="classendstone_1_1_inventory_1a70dc622a7f4ecaf2e6bae130a2326f78">
    <td>[`addItem`](#function-additem-12)</td>
    <td>Stores the given ItemStacks in the inventory.</td>
</tr>
<tr key="classendstone_1_1_inventory_1adb3dc76d1831c648ce06f265ea58ac23">
    <td>[`addItem`](#function-additem-22)</td>
    <td>Stores the given ItemStacks in the inventory.</td>
</tr>
<tr key="classendstone_1_1_inventory_1a57fc0a3dcecac9bfe137b3072239764d">
    <td>[`all`](#function-all-12)</td>
    <td>Finds all slots in the inventory containing any ItemStacks with the given ItemType .</td>
</tr>
<tr key="classendstone_1_1_inventory_1abbe720b7ba894612f866a8cf060077ae">
    <td>[`all`](#function-all-22)</td>
    <td>Finds all slots in the inventory containing any ItemStacks with the given ItemStack .</td>
</tr>
<tr key="classendstone_1_1_inventory_1a367187fb26cad50974f6a01f0df9e86c">
    <td>[`clear`](#function-clear-12)</td>
    <td>Clears out a particular slot in the index.</td>
</tr>
<tr key="classendstone_1_1_inventory_1afc114b9f2e5c05fb1e1388963b8c86e8">
    <td>[`clear`](#function-clear-22)</td>
    <td>Clears out the whole Inventory .</td>
</tr>
<tr key="classendstone_1_1_inventory_1a621dd957d66ba16baf072ccc5e3ac55a">
    <td>[`contains`](#function-contains-13)</td>
    <td>Checks if the inventory contains any ItemStacks with the given ItemType .</td>
</tr>
<tr key="classendstone_1_1_inventory_1a5c25510c8e1b29286608bc04b42512dc">
    <td>[`contains`](#function-contains-23)</td>
    <td>Checks if the inventory contains any ItemStacks with the given ItemStack .</td>
</tr>
<tr key="classendstone_1_1_inventory_1afdc19f459553b5b6fa058600e9aea8f1">
    <td>[`contains`](#function-contains-33)</td>
    <td>Checks if the inventory contains at least the minimum amount specified of exactly matching ItemStacks.</td>
</tr>
<tr key="classendstone_1_1_inventory_1a780dcea7873db6b997799f1a65cda982">
    <td>[`containsAtLeast`](#function-containsatleast-12)</td>
    <td>Checks if the inventory contains any ItemStacks with the given ItemType , adding to at least the minimum amount specified.</td>
</tr>
<tr key="classendstone_1_1_inventory_1a1ad7af2b4f4e7ae60e1c9067e8e0af29">
    <td>[`containsAtLeast`](#function-containsatleast-22)</td>
    <td>Checks if the inventory contains ItemStacks matching the given ItemStack whose amounts sum to at least the minimum amount specified.</td>
</tr>
<tr key="classendstone_1_1_inventory_1a7e262d30e950644ab36e84ad346e83d6">
    <td>[`first`](#function-first-12)</td>
    <td>Finds the first slot in the inventory containing an ItemStack with the given ItemType .</td>
</tr>
<tr key="classendstone_1_1_inventory_1afd3997dd60d7f4f0649aa4280f05bc24">
    <td>[`first`](#function-first-22)</td>
    <td>Returns the first slot in the inventory containing an ItemStack with the given stack.</td>
</tr>
<tr key="classendstone_1_1_inventory_1a26fb3d2b14213b78a13a0cc46d9a6128">
    <td>[`firstEmpty`](#function-firstempty)</td>
    <td>Returns the first empty Slot.</td>
</tr>
<tr key="classendstone_1_1_inventory_1aed3238de786bddbcf39761658b95c48b">
    <td>[`getContents`](#function-getcontents)</td>
    <td>Returns all ItemStacks from the inventory.</td>
</tr>
<tr key="classendstone_1_1_inventory_1ade917e3c7719cb3ec7b6ffa8db27462b">
    <td>[`getItem`](#function-getitem)</td>
    <td>Returns the ItemStack found in the slot at the given index.</td>
</tr>
<tr key="classendstone_1_1_inventory_1ae57598046e6b5df98a7710d91953a823">
    <td>[`getMaxStackSize`](#function-getmaxstacksize)</td>
    <td>Returns the maximum stack size for an ItemStack in this inventory.</td>
</tr>
<tr key="classendstone_1_1_inventory_1aeb95253ae2eaa18839b05b3612a7007e">
    <td>[`getSize`](#function-getsize)</td>
    <td>Returns the size of the inventory.</td>
</tr>
<tr key="classendstone_1_1_inventory_1ade31b1be3db4e11cadda1767a419c62c">
    <td>[`isEmpty`](#function-isempty)</td>
    <td>Check whether this inventory is empty. An inventory is considered to be empty if there are no ItemStacks in any slot of this inventory.</td>
</tr>
<tr key="classendstone_1_1_inventory_1a6a34081f5a017d312d4aa43f5c94c197">
    <td>[`remove`](#function-remove-12)</td>
    <td>Removes all stacks in the inventory matching the given ItemType .</td>
</tr>
<tr key="classendstone_1_1_inventory_1af3402b278d0775d48a56593bf49ee52e">
    <td>[`remove`](#function-remove-22)</td>
    <td>Removes all stacks in the inventory matching the given stack.</td>
</tr>
<tr key="classendstone_1_1_inventory_1a7cc5b3f5922baff4b878398a21ded738">
    <td>[`removeItem`](#function-removeitem-12)</td>
    <td>Removes the given ItemStacks from the inventory.</td>
</tr>
<tr key="classendstone_1_1_inventory_1a2f4af3f93e53605d1c1540084305fdf4">
    <td>[`removeItem`](#function-removeitem-22)</td>
    <td>Removes the given ItemStacks from the inventory.</td>
</tr>
<tr key="classendstone_1_1_inventory_1a44f969fa5f94ddd9c44a94ea7b1643d8">
    <td>[`setContents`](#function-setcontents)</td>
    <td>Completely replaces the inventory's contents. Removes all existing contents and replaces it with the ItemStacks given in the array.</td>
</tr>
<tr key="classendstone_1_1_inventory_1ad1d02b72943819764794ebc3ccb66e4c">
    <td>[`setItem`](#function-setitem)</td>
    <td>Stores the ItemStack at the given index of the inventory.</td>
</tr>
<tr key="classendstone_1_1_inventory_1ae4195380f173c841a5df5ddea12087ae">
    <td>[`~Inventory`](#function-inventory)</td>
    <td></td>
</tr>
</tbody>
</table>

### <code className="doc-symbol doc-symbol-function"/> addItem

```cpp
virtual std::unordered_map< int, ItemStack * > endstone::Inventory::addItem (
    std::vector< ItemStack * > items
) = 0
```

Stores the given ItemStacks in the inventory.



**Note:**

This will try to fill existing stacks and empty slots as well as it can.




**Note:**

The returned map contains what it couldn't store, where the key is the index, and the value is the [**ItemStack**](classendstone_1_1_item_stack.md). If all items are stored, it will return an empty map.




**Parameters:**


* `items` The ItemStacks to add



**Returns:**

A map containing items that couldn't be added. 





        
### <code className="doc-symbol doc-symbol-function"/> addItem

```cpp
template<typename... Args, typename>
inline std::unordered_map< int, ItemStack * > endstone::Inventory::addItem (
    Args &&... items
) 
```

Stores the given ItemStacks in the inventory.



**Note:**

This will try to fill existing stacks and empty slots as well as it can.




**Note:**

The returned map contains what it couldn't store, where the key is the index, and the value is the [**ItemStack**](classendstone_1_1_item_stack.md). If all items are stored, it will return an empty map.




**Parameters:**


* `items` The ItemStacks to add



**Returns:**

A map containing items that couldn't be added. 





        
### <code className="doc-symbol doc-symbol-function"/> all

```cpp
virtual std::unordered_map< int, std::unique_ptr< ItemStack > > endstone::Inventory::all (
    const std::string & type
) const = 0
```

Finds all slots in the inventory containing any ItemStacks with the given ItemType .

The returned map contains entries where, the key is the slot index, and the value is the [**ItemStack**](classendstone_1_1_item_stack.md) in that slot. If no matching [**ItemStack**](classendstone_1_1_item_stack.md) is found, an empty map is returned.




**Parameters:**


* `type` The [**ItemType**](classendstone_1_1_item_type.md) to match against



**Returns:**

A map from slot indexes to item at index 





        
### <code className="doc-symbol doc-symbol-function"/> all

```cpp
virtual std::unordered_map< int, std::unique_ptr< ItemStack > > endstone::Inventory::all (
    const ItemStack & item
) const = 0
```

Finds all slots in the inventory containing any ItemStacks with the given ItemStack .



**Note:**

This will only match slots if both the type and the amount of the stack match


The returned map contains entries where, the key is the slot index, and the value is the [**ItemStack**](classendstone_1_1_item_stack.md) in that slot. If no matching [**ItemStack**](classendstone_1_1_item_stack.md) is found, an empty map is returned.




**Parameters:**


* `item` The [**ItemStack**](classendstone_1_1_item_stack.md) to match against



**Returns:**

A map from slot indexes to item at index 





        
### <code className="doc-symbol doc-symbol-function"/> clear

```cpp
virtual void endstone::Inventory::clear (
    int index
) = 0
```

Clears out a particular slot in the index.



**Parameters:**


* `index` The index to empty. 




        
### <code className="doc-symbol doc-symbol-function"/> clear

```cpp
virtual void endstone::Inventory::clear () = 0
```

Clears out the whole Inventory .

### <code className="doc-symbol doc-symbol-function"/> contains

```cpp
virtual bool endstone::Inventory::contains (
    const std::string & type
) const = 0
```

Checks if the inventory contains any ItemStacks with the given ItemType .



**Parameters:**


* `type` The item type to check for



**Returns:**

true if an [**ItemStack**](classendstone_1_1_item_stack.md) is found with the given [**ItemType**](classendstone_1_1_item_type.md) 





        
### <code className="doc-symbol doc-symbol-function"/> contains

```cpp
virtual bool endstone::Inventory::contains (
    const ItemStack & item
) const = 0
```

Checks if the inventory contains any ItemStacks with the given ItemStack .



**Note:**

This will only return true if both the type and the amount of the stack match.




**Parameters:**


* `item` The [**ItemStack**](classendstone_1_1_item_stack.md) to match against 



**Returns:**

true if any exactly matching ItemStacks were found, false otherwise 





        
### <code className="doc-symbol doc-symbol-function"/> contains

```cpp
virtual bool endstone::Inventory::contains (
    const ItemStack & item,
    int amount
) const = 0
```

Checks if the inventory contains at least the minimum amount specified of exactly matching ItemStacks.



**Note:**

An [**ItemStack**](classendstone_1_1_item_stack.md) only counts if both the type and the amount of the stack match.




**Parameters:**


* `item` the [**ItemStack**](classendstone_1_1_item_stack.md) to match against 
* `amount` how many identical stacks to check for



**Returns:**

true if amount less than 1 or if amount of exactly matching ItemStacks were found, false otherwise 





        
### <code className="doc-symbol doc-symbol-function"/> containsAtLeast

```cpp
virtual bool endstone::Inventory::containsAtLeast (
    const std::string & type,
    int amount
) const = 0
```

Checks if the inventory contains any ItemStacks with the given ItemType , adding to at least the minimum amount specified.



**Parameters:**


* `type` The [**ItemType**](classendstone_1_1_item_type.md) to check for 
* `amount` The minimum amount



**Returns:**

true if amount is less than 1, true if enough ItemStacks were found to add to the given amount 





        
### <code className="doc-symbol doc-symbol-function"/> containsAtLeast

```cpp
virtual bool endstone::Inventory::containsAtLeast (
    const ItemStack & item,
    int amount
) const = 0
```

Checks if the inventory contains ItemStacks matching the given ItemStack whose amounts sum to at least the minimum amount specified.



**Parameters:**


* `item` the [**ItemStack**](classendstone_1_1_item_stack.md) to match against 
* `amount` the minimum amount



**Returns:**

true if amount less than 1 or enough ItemStacks were found to add to the given amount, false otherwise 





        
### <code className="doc-symbol doc-symbol-function"/> first

```cpp
virtual int endstone::Inventory::first (
    const std::string & type
) const = 0
```

Finds the first slot in the inventory containing an ItemStack with the given ItemType .



**Parameters:**


* `type` The [**ItemType**](classendstone_1_1_item_type.md) to look for



**Returns:**

The slot index of the given [**ItemType**](classendstone_1_1_item_type.md) or -1 if not found 





        
### <code className="doc-symbol doc-symbol-function"/> first

```cpp
virtual int endstone::Inventory::first (
    const ItemStack & item
) const = 0
```

Returns the first slot in the inventory containing an ItemStack with the given stack.



**Parameters:**


* `item` The [**ItemStack**](classendstone_1_1_item_stack.md) to match against



**Returns:**

The slot index of the given [**ItemStack**](classendstone_1_1_item_stack.md) or -1 if not found 





        
### <code className="doc-symbol doc-symbol-function"/> firstEmpty

```cpp
virtual int endstone::Inventory::firstEmpty () const = 0
```

Returns the first empty Slot.



**Returns:**

The first empty Slot found, or -1 if no empty slots. 





        
### <code className="doc-symbol doc-symbol-function"/> getContents

```cpp
virtual std::vector< std::unique_ptr< ItemStack > > endstone::Inventory::getContents () const = 0
```

Returns all ItemStacks from the inventory.



**Returns:**

An array of ItemStacks from the inventory. Individual items may be null. 





        
### <code className="doc-symbol doc-symbol-function"/> getItem

```cpp
virtual std::unique_ptr< ItemStack > endstone::Inventory::getItem (
    int index
) const = 0
```

Returns the ItemStack found in the slot at the given index.



**Parameters:**


* `index` The index of the Slot's [**ItemStack**](classendstone_1_1_item_stack.md) to return



**Returns:**

The [**ItemStack**](classendstone_1_1_item_stack.md) in the slot 





        
### <code className="doc-symbol doc-symbol-function"/> getMaxStackSize

```cpp
virtual int endstone::Inventory::getMaxStackSize () const = 0
```

Returns the maximum stack size for an ItemStack in this inventory.



**Returns:**

The maximum size for an [**ItemStack**](classendstone_1_1_item_stack.md) in this inventory. 





        
### <code className="doc-symbol doc-symbol-function"/> getSize

```cpp
virtual int endstone::Inventory::getSize () const = 0
```

Returns the size of the inventory.



**Returns:**

The size of the inventory 





        
### <code className="doc-symbol doc-symbol-function"/> isEmpty

```cpp
virtual bool endstone::Inventory::isEmpty () const = 0
```

Check whether this inventory is empty. An inventory is considered to be empty if there are no ItemStacks in any slot of this inventory.



**Returns:**

true if empty, false otherwise 





        
### <code className="doc-symbol doc-symbol-function"/> remove

```cpp
virtual void endstone::Inventory::remove (
    const std::string & type
) = 0
```

Removes all stacks in the inventory matching the given ItemType .



**Parameters:**


* `type` The [**ItemType**](classendstone_1_1_item_type.md) to remove 




        
### <code className="doc-symbol doc-symbol-function"/> remove

```cpp
virtual void endstone::Inventory::remove (
    const ItemStack & item
) = 0
```

Removes all stacks in the inventory matching the given stack.



**Note:**

This will only match a slot if both the type and the amount of the stack match




**Parameters:**


* `item` The [**ItemStack**](classendstone_1_1_item_stack.md) to match against 




        
### <code className="doc-symbol doc-symbol-function"/> removeItem

```cpp
virtual std::unordered_map< int, ItemStack * > endstone::Inventory::removeItem (
    std::vector< ItemStack * > items
) = 0
```

Removes the given ItemStacks from the inventory.



**Note:**

It will try to remove 'as much as possible' from the types and amounts you give as arguments.




**Note:**

The returned HashMap contains what it couldn't remove, where the key is the index, and the value is the [**ItemStack**](classendstone_1_1_item_stack.md). If all the given ItemStacks are removed, it will return an empty map.




**Parameters:**


* `items` The ItemStacks to remove



**Returns:**

A map containing items that couldn't be removed. 





        
### <code className="doc-symbol doc-symbol-function"/> removeItem

```cpp
template<typename... Args, typename>
inline std::unordered_map< int, ItemStack * > endstone::Inventory::removeItem (
    Args &&... items
) 
```

Removes the given ItemStacks from the inventory.



**Note:**

It will try to remove 'as much as possible' from the types and amounts you give as arguments.




**Note:**

The returned HashMap contains what it couldn't remove, where the key is the index, and the value is the [**ItemStack**](classendstone_1_1_item_stack.md). If all the given ItemStacks are removed, it will return an empty map.




**Parameters:**


* `items` The ItemStacks to remove 



**Returns:**

A map containing items that couldn't be removed. 





        
### <code className="doc-symbol doc-symbol-function"/> setContents

```cpp
virtual Result< void > endstone::Inventory::setContents (
    std::vector< const ItemStack * > items
) = 0
```

Completely replaces the inventory's contents. Removes all existing contents and replaces it with the ItemStacks given in the array.



**Parameters:**


* `items` A complete replacement for the contents; the length must be less than or equal to [**getSize()**](classendstone_1_1_inventory.md#function-getsize). 




        
### <code className="doc-symbol doc-symbol-function"/> setItem

```cpp
virtual void endstone::Inventory::setItem (
    int index,
    const ItemStack * item
) = 0
```

Stores the ItemStack at the given index of the inventory.



**Parameters:**


* `index` The index where to put the [**ItemStack**](classendstone_1_1_item_stack.md) 
* `item` The [**ItemStack**](classendstone_1_1_item_stack.md) to set 




        
### <code className="doc-symbol doc-symbol-function"/> ~Inventory

```cpp
virtual endstone::Inventory::~Inventory () = default
```

