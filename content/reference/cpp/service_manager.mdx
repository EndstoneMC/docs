---
title: "ServiceManager"
---

## <code className="doc-symbol doc-symbol-class"/> endstone::ServiceManager

















**Functions:**

<table>
<thead>
   <tr>
      <th>Name</th>
      <th>Description</th>
   </tr>
</thead>
<tbody>
<tr key="classendstone_1_1_service_manager_1a329a71e32b64af8ed4df189f3976739f">
    <td>[`get`](#get)</td>
    <td>Queries for a provider. This may return null if no provider has been registered for a service. The highest priority provider is returned.</td>
</tr>
<tr key="classendstone_1_1_service_manager_1ac2fcb33a1d0fc89902100b7244fa55b0">
    <td>[`load`](#load)</td>
    <td></td>
</tr>
<tr key="classendstone_1_1_service_manager_1a8fbe97b8dcbc297e6386f0e6c1c6e3f6">
    <td>[`registerService`](#registerService)</td>
    <td>Register a provider of a service.</td>
</tr>
<tr key="classendstone_1_1_service_manager_1ad6e9b9f07037a0fa971697bf9de486ec">
    <td>[`unregister`](#unregister)</td>
    <td>Unregister a particular provider for a particular service.</td>
</tr>
<tr key="classendstone_1_1_service_manager_1aa8f39bf20330d57e8d3d242645ef5109">
    <td>[`unregister`](#unregister)</td>
    <td>Unregister a particular provider.</td>
</tr>
<tr key="classendstone_1_1_service_manager_1af62f98b2abdd3fa261d372dd10b5d831">
    <td>[`unregisterAll`](#unregisterAll)</td>
    <td>Unregister all the services registered by a particular plugin.</td>
</tr>
</tbody>
</table>

### <code className="doc-symbol doc-symbol-function"/> get [#get]

```cpp
virtual std::shared_ptr< Service > endstone::ServiceManager::get (
    std::string name
) const = 0
```

Queries for a provider. This may return null if no provider has been registered for a service. The highest priority provider is returned.





**Parameters:**


* `name` The service name 



**Returns:**

provider or null 





        
### <code className="doc-symbol doc-symbol-function"/> load [#load]

```cpp
template<typename T>
inline std::shared_ptr< T > endstone::ServiceManager::load (
    std::string name
) const
```
### <code className="doc-symbol doc-symbol-function"/> registerService [#registerService]

```cpp
virtual void endstone::ServiceManager::registerService (
    std::string name,
    std::shared_ptr< Service > provider,
    const Plugin & plugin,
    ServicePriority priority
) = 0
```

Register a provider of a service.





**Parameters:**


* `name` service name 
* `provider` service provider to register 
* `plugin` plugin associated with the service 
* `priority` priority of the provider 




        
### <code className="doc-symbol doc-symbol-function"/> unregister [#unregister]



```cpp
virtual void endstone::ServiceManager::unregister (
    std::string name,
    const Service & provider
) = 0
```

Unregister a particular provider for a particular service.





**Parameters:**


* `name` The service name 
* `provider` The service provider implementation 




        

```cpp
virtual void endstone::ServiceManager::unregister (
    const Service & provider
) = 0
```

Unregister a particular provider.





**Parameters:**


* `provider` The service provider implementation 




        
### <code className="doc-symbol doc-symbol-function"/> unregisterAll [#unregisterAll]

```cpp
virtual void endstone::ServiceManager::unregisterAll (
    const Plugin & plugin
) = 0
```

Unregister all the services registered by a particular plugin.





**Parameters:**


* `plugin` The plugin 




        