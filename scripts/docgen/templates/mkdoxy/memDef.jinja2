---
brief: True
details: True
implements: True
url_source: True
---

{# This template is used inside of member.py #}
{%- filter indent(config.get('indent_level', 0), True) -%}

### <code className="doc-symbol doc-symbol-{{node.kind.value}}"/> {{node.name_long if node.is_group else node.name_short}}

{{node.codeblock | use_code_language("cpp")}}

{% if configMemDef.get('brief') -%}
{% if node.has_brief -%}
{{node._brief.plain() + "\n"}}
{%- endif -%}
{%- endif -%}

{%- if configMemDef.get('details') -%}
{%- if node.has_details -%}
{{node.details | use_code_language("cpp")}}
{%- endif -%}
{%- endif -%}

{% if configMemDef.get('implements') -%}
{% if node.reimplements %}
Implements [*{{node.reimplements.name_long}}*]({{node.reimplements.url}})
{% endif %}
{%- endif -%}

{% endfilter %}